# Привет, сообщество **FlowLink Proxy**! 🚀

Версия **0.1.2** — третий публичный релиз.

---

## ✨ Возможности
- **Автоматическая маршрутизация трафика:** сайты по маскам — через SOCKS5-прокси с паролем, остальные — напрямую.
- **Аутентификация на бэкенде:** Chrome не умеет передавать логин/пароль в SOCKS5 — FlowLink Proxy берёт аутентификацию на себя, а браузер работает через обычный HTTP-прокси `localhost:8080`.
- **Chrome-расширение (Manifest V3):** управление прокси и масками, уведомления, проверка обновлений, черновики форм.
- **Системный трей:** кастомное тёмное меню с быстрым доступом к настройкам.
- **Шифрование конфигурации:** AES-256-GCM + PBKDF2-HMAC-SHA256 с ротацией ключа.
- **Локализация:** русский, английский, сербский (расширение и бэкенд).
- **Автозапуск с системой:** Windows, Linux (systemd/desktop), macOS (LaunchAgent).

---

## 🆕 Что нового в 0.1.2
- **Автодетект браузера Vivaldi** во всех ОС (Windows: реестр App Paths, per-user `%LOCALAPPDATA%`, `%ProgramFiles%` и `%ProgramFiles(x86)%`; Linux: `which vivaldi`; macOS: `/Applications/Vivaldi.app`).
- **Исправлен автодетект Brave на Windows**: проверялось только `%ProgramFiles%`, а установка по умолчанию идёт в `%ProgramFiles(x86)%` — браузер не находился.
- **Справка расширения:** вкладка «Настройка порта» переименована в «Порты»; в футеры «Не удалось решить проблему?» добавлены ссылки на README и SETUP.md.
- **Документация переработана:** актуализированы имена артефактов и пути, исправлены URL; дерево структуры и инструкции для разработчиков перенесены в DEBUG.md; в README/SETUP порядок установки расширения — `.crx` выше `.zip`.

---

## 📦 Платформы
| Платформа | Формат |
|-----------|--------|
| Windows x64 | `.exe` (Inno Setup установщик) |
| Linux x64 | `.tar.gz`, `.deb`, `.rpm` |
| macOS x64 | `.tar.gz`, `.pkg` |
| macOS arm64 | `.tar.gz`, `.pkg` |
| Все платформы | `.crx`, `.zip` (Chrome расширение) |

---

## 📲 Как установить?
1. Скачайте установщик для вашей платформы внизу этой страницы.
2. Установите Chrome-расширение из файла `.crx` или распакуйте `.zip` и загрузите его как распакованное расширение.

> ⚠️ **Важно:** пока расширение не опубликовано в Opera Add-ons, его нужно устанавливать вручную. Подробная инструкция по установке — в [SETUP.md](https://github.com/FlowHack/flowlink-proxy/blob/develop/SETUP.md).

---

## 🔧 Исправления
- **Автодетект браузеров:** добавлен Vivaldi (все ОС), починено нахождение Brave на Windows (x86 и per-user `%LOCALAPPDATA%`) — браузер не находился по умолчанию.
- **Документация:** исправлены битые URL релизов и `git clone`, неверные имена артефактов (`deb`/`rpm`/`pkg`), пути сборки; устранены дубли (сборка, устранение проблем, выбор браузера); Firefox исключён из списка поддерживаемых.
- **Расширение:** вкладка «Настройка порта» → «Порты»; футеры справки дополнены ссылками на README/SETUP.md.

---

**Разработчик:** @FlowHack
**Build:** `v0.1.2`