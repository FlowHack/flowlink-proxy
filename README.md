# FlowLink Proxy

Управление SOCKS5-прокси с масками адресов. Расширение для Chrome (Manifest V3).

## Установка

1. Открой `chrome://extensions`
2. Включи **Режим разработчика**
3. Нажми **Загрузить распакованное расширение**
4. Выбери папку `src/`

## Использование

1. Нажми на иконку расширения → откроется popup
2. Нажми **+ Добавить прокси** → заполни IP, порт, логин и пароль (все поля обязательны)
3. После добавления прокси по умолчанию включён
4. Нажми на IP прокси → откроется управление **масками** (регулярные выражения для URL)
5. Маски определяют, какие сайты направляются через этот прокси
6. При открытии popup автоматически запускается пинг всех прокси (если расширение включено)

### Возможности

- **Пинг всех** — проверяет доступность всех прокси (последовательно, 5 сек таймаут)
- **Авто-пинг при открытии popup** — статус виден без ручного клика
- **Глобальный переключатель** — мгновенно отключает/включает всю маршрутизацию
- **Проверка при входе на сайт** — при навигации на URL из маски проверяется прокси; если недоступен — трафик блокируется (Strict Proxy Policy)
- **Failover** — при недоступности прокси трафик блокируется (Strict Proxy Policy), при восстановлении разблокируется
- **Уведомления** — при блокировке/восстановлении прокси
- **Авто-экранирование масок** — `*` → `.*`, неэкранированная `.` → `\.` для удобного ввода

## Структура проекта

```
src/
├── manifest.json              # Манифест MV3
├── shared/
│   ├── constants.js           # Ключи storage, валидаторы (IP, port)
│   └── messages.js            # Константы сообщений UI↔Background
├── background/
│   ├── service-worker.js      # Точка входа, инициализация
│   ├── crypto-service.js      # AES-GCM 256 шифрование
│   ├── data-controller.js     # CRUD ProxyTable + MaskTable
│   ├── mask-cache.js          # Memory cache с RegExp для быстрого поиска
│   ├── proxy-controller.js    # PAC-скрипт, Failover, пинг, монитор навигации
│   └── message-router.js      # Маршрутизатор сообщений от Popup
├── popup/
│   ├── popup.html             # Интерфейс
│   ├── popup.css              # Тёмная тема
│   └── popup.js               # Контроллер UI
└── icons/
    ├── icon48.png
    └── icon128.png
```

## Безопасность

- **Мастер-ключ** генерируется при первом запуске, хранится в `chrome.storage.local` в формате JWK
- Username/password шифруются **AES-GCM 256** с уникальным 12-байтным IV
- Расшифровка только в Service Worker — пароли никогда не покидают защищённый контекст
- CSP: `script-src 'self'; object-src 'self'` — запрещены eval, unsafe-inline, unsafe-eval
- PAC-скрипт генерируется через `JSON.stringify` — защита от XSS-инъекций
- Нарушение Strict Proxy Policy: при недоступности прокси возвращается `PROXY 0.0.0.0:9` (блокировка)

## Технические детали

- **Тест прокси**: через реальную вкладку браузера (`chrome.tabs.create`, `active: false`). В MV3 это единственный способ выполнить запрос, проходящий через `chrome.proxy.settings` и корректно обрабатывающий SOCKS5-аутентификацию. Extension-контексты (SW, popup, offscreen) обходят системный прокси.
- **Событийный пинг**: Health Check по таймеру (`chrome.alarms`) **не используется**. Проверки происходят только при открытии popup, нажатии «Пинг всех», и навигации на URL из маски.
- **При выключенном расширении** пинг не запускается, монитор навигации отключён.
