# --- Конфигурация агента (секреты, промпты, лог разработки) ---
/myAgents/
AI_DEV_LOG.md
AGENTS*
CONVENTIONS*
.aider*
aider*
.opencodeignore
litellm*
.opencode/
opencode*
task*
/lancedb/

# --- IDE и редакторы ---
.idea/
.vscode/
*.iml
.gradle/

# --- Системные файлы ---
.DS_Store
Thumbs.db

# --- Python кэш и сборка ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.python-version
.tox/
/build/
dist/
*.egg
.eggs/

# --- Логи ---
logs/
*.log

# --- CRX private key (не коммитить!) ---
scripts/build/crx-private-key.pem
*.crx

# --- Конфигурация прокси (пароли в открытом виде не хранятся) ---
config.json
config.local.json
.flowlink.key
.flowlink.salt
.flowlink-port
.flowlink-settings

# --- PyInstaller / сборка standalone ---
server/work/
server/dist/
server/FlowLink Proxy/
build-tmp/
*.spec
# Исключение: RPM spec-файл проекта (в отличие от генерируемых PyInstaller'ом .spec-файлов)
!scripts/build/flowlink.spec

# --- Сборка релизов ---
releases/
scripts/installer/Output/

# --- Сборка пакетов ---
*.deb
*.rpm
*.pkg
*.tar.gz

# --- Покрытие тестов ---
.coverage
htmlcov/

# --- Сгенерированные иконки (только .ico — производные от .png) ---
extension/icons/*.ico

# --- Секреты и окружение ---
.env
.env.*
*.local
*.secret
crx-private-key.pem
